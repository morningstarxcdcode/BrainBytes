import '@testing-library/jest-dom'

// If you need global mocks (e.g., local storage, ResizeObserver), add them here
// Example: globalThis.ResizeObserver = class { observe() {} disconnect() {} unobserve() {} }
